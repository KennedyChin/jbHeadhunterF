import { InputFocusWithLabelDirective } from './input-focus-with-label.directive';

describe('InputFocusWithLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFocusWithLabelDirective();
    expect(directive).toBeTruthy();
  });
});
