import { NewLineMePipe } from './new-line-me.pipe';

describe('NewLineMePipe', () => {
  it('create an instance', () => {
    const pipe = new NewLineMePipe();
    expect(pipe).toBeTruthy();
  });
});
